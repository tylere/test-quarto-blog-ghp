---
title: "Post With Code"
author: "Harlow Malloc"
date: "2024-06-15"
categories: [news, code, analysis]
image: "image.jpg"
---

This is a post with executable code.

```{python}
1 + 1
```

The code below creates a figure.
```{python}
#| label: fig-polar
#| fig-cap: "A line plot on a polar axis, just testing"

import numpy as np
import matplotlib.pyplot as plt

r = np.arange(0, 2, 0.01)
theta = 2 * np.pi * r
fig, ax = plt.subplots(
  subplot_kw = {'projection': 'polar'} 
)
ax.plot(theta, r)
ax.set_rticks([0.5, 1, 1.5, 2])
ax.grid(True)
plt.show()
```